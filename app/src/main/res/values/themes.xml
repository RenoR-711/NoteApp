<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NotesApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/mist</item>
        <item name="colorPrimaryVariant">@color/mist</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/stone</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/material_dynamic_neutral40</item>

        <!-- Customize your theme here. -->

    </style>
    <style name="Theme.NotesApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/stone</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/branding_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.NotesApp</item>
        <item name="windowSplashScreenAnimationDuration">600</item> <!-- Animation Start -->
    </style>
</resources>