<resources>
    <!-- Base application theme. -->
    <style name="Theme.NotesApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/mist</item>
        <item name="colorPrimaryVariant">@color/mist</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/stone</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/material_dynamic_neutral40</item>

        <!-- Customize your theme here. -->

    </style>
    <!-- Theme für SplashActivity -->
    <style name="Theme.NotesApp.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Kein ActionBar oder Titel -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Transparente Statusleiste für Fullscreen-GIF -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>